
apply plugin: "maven-publish"
apply plugin: "signing"

task sourceJar(type: Jar) {
    archiveClassifier = 'sources'
    from sourceSets.main.allJava
}
task javadocJar(type: Jar) {
    archiveClassifier = 'javadoc'
    from javadoc.destinationDir
}
publishing {
    publications {
        oss(MavenPublication) {
            pom {
                name = "SIMPLE AUDIT: ${project.name}"
                description = "${project.description}"
                url = 'https://github.com/yxc023/simple-audit'
                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://mit-license.org/'
                    }
                }
                developers {
                    developer {
                        id = 'yxc023'
                        name = 'Michael Yang'
                        email = 'yxc023@qq.com'
                    }
                }
                scm {
                    connection = 'scm:git:git://github.com:yxc023/simple-audit.git'
                    developerConnection = 'scm:git:git://github.com:yxc023/simple-audit.git'
                    url = 'https://github.com/yxc023/simple-audit'
                }
            }
            from components.java
            artifact sourceJar
            artifact javadocJar

        }
    }
    repositories {
        maven {
            name "snapshot"
            url "https://oss.sonatype.org/content/repositories/snapshots"
            credentials {
                username property('oss.publish.username')
                password property('oss.publish.password')
            }
        }
        maven {
            name "release"
            url "https://oss.sonatype.org/service/local/staging/deploy/maven2/"
            credentials {
                username property('oss.publish.username')
                password property('oss.publish.password')
            }
        }

        maven {
            name "self.release"
            url property('oss.self.release.url')
            credentials {
                username property('oss.self.username')
                password property('oss.self.password')
            }
        }

        maven {
            name "self.snapshot"
            url property('oss.self.snapshot.url')
            credentials {
                username property('oss.self.username')
                password property('oss.self.password')
            }
        }
    }
}

signing {
    sign publishing.publications.oss
}
